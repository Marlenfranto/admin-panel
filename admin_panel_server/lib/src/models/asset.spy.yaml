class: Asset
table: asset
fields:
  organization: Organization?, relation(optional)
  version: String
  module: String
  name: String, default='', !persist
  description: String?, !persist
  url: String, default='', !persist
