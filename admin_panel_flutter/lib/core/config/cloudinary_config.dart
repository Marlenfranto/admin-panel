/// Cloudinary credentials.
///
/// Set [cloudName] and [uploadPreset] from your Cloudinary dashboard:
///   https://cloudinary.com/console
///
/// [uploadPreset] must be an **unsigned** preset (Settings → Upload → Upload presets).
class CloudinaryConfig {
  CloudinaryConfig._();

  static const cloudName    = 'dkghfsvsi';
  static const uploadPreset = 'firesafex';
}
